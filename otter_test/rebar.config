{erl_opts, [debug_info]}.

{plugins, [rebar3_otter]}.

{provider_hooks, [
    {pre, [{compile, otter_compile}, {clean, otter_clean}]}
]}.

{otter_crates, [
    #{name => "otter_test", path => "native/otter_test"}
]}.
